{
  "name": "shared_types",
  "version": "1.0.0",
  "description": "Shared interfaces between backend and client",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "rebuild": "node scripts/fetch-pokemon.js --force",
    "prebuild": "node scripts/fetch-pokemon.js",
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
